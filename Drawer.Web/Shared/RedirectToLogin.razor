@using Drawer.Web.Pages
@using Drawer.Web.Utils

@code {
    protected override Task OnInitializedAsync()
    {
        // 로그인 후 로그인 페이지 리디렉트를 생성한 곳으로 다시 돌아간다
        NavManager.NavigateTo(Paths.Account.Login.AddQuery("redirectUri", NavManager.Uri));

        return Task.CompletedTask;
    }
}